fastapi==0.109.0
uvicorn[standard]==0.27.0
gunicorn==21.2.0
transformers==4.37.0
# BGE-M3 uses an XLM-RoBERTa SentencePiece tokenizer (needs sentencepiece + protobuf)
sentencepiece==0.2.0
protobuf>=4.25.0
pydantic==2.5.0
prometheus-client==0.20.0
python-dotenv==1.0.0
# torch: installed in Dockerfile from PyTorch CPU wheel index (smaller than default PyPI CUDA build)
httpx>=0.27.0
# ONNX Runtime for cross-encoder reranker (bge-reranker-base INT8)
onnxruntime>=1.17.0
optimum[onnxruntime]>=1.17.0
# East-west gRPC server (embedding.v1); stubs are committed (protos/gen_python.sh)
grpcio>=1.62.0

