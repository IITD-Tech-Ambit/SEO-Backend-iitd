fastapi==0.109.0
uvicorn[standard]==0.27.0
gunicorn==21.2.0
transformers==4.37.0
# BGE-M3 uses an XLM-RoBERTa SentencePiece tokenizer (needs sentencepiece + protobuf)
sentencepiece==0.2.0
protobuf>=4.25.0
pydantic==2.5.0
prometheus-client==0.20.0
python-dotenv==1.0.0
# torch: installed in Dockerfile from PyTorch CPU wheel index (smaller than default PyPI CUDA build)
httpx>=0.27.0
# ONNX Runtime for cross-encoder reranker (bge-reranker-base INT8)
onnxruntime>=1.17.0
optimum[onnxruntime]>=1.17.0
# East-west gRPC server (embedding.v1); stubs come from proto-registry (see below),
# not a committed copy.
grpcio>=1.62.0
# Generated gRPC stubs (embedding.v1, etc.) from the shared proto-registry repo.
# Pinned to a release/vX.Y.Z branch (immutable tags hold source .proto only; the
# generated Python lives on the matching release branch — see that repo's README).
iitd-tech-ambit-proto-stubs @ git+https://github.com/IITD-Tech-Ambit/proto-registry.git@release/v0.1.0#subdirectory=gen-python

